"use client"
import { AnimatePresence,motion,useReducedMotion } from "framer-motion"
import { Trophy } from "lucide-react"
export type AchievementData={key:string;title:string;detail?:string}
export function Achievement({achievement}:{achievement:AchievementData|null}){const reduced=useReducedMotion();return <AnimatePresence>{achievement&&<motion.div key={achievement.key} role="status" aria-live="polite" initial={reduced?false:{opacity:0,y:24,scale:.96}} animate={{opacity:1,y:0,scale:1}} exit={reduced?{opacity:0}:{opacity:0,y:16,scale:.98}} className="fixed bottom-[max(1rem,env(safe-area-inset-bottom))] left-1/2 z-[100] w-[calc(100%-2rem)] max-w-sm -translate-x-1/2 overflow-hidden rounded-xl border border-primary/40 bg-card/95 p-4 shadow-2xl backdrop-blur md:left-auto md:right-5 md:translate-x-0"><div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-primary via-cyan-300 to-primary"/><div className="flex items-center gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-primary/15 text-primary"><Trophy size={20}/></span><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-muted-foreground">Achievement unlocked</p><p className="font-space-grotesk font-bold">{achievement.title}</p>{achievement.detail&&<p className="text-xs text-muted-foreground">{achievement.detail}</p>}</div></div></motion.div>}</AnimatePresence>}
